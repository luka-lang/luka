type size_t = u64;
type ssize_t = s64;

extern close(fd: s32);
extern read(fd: s32, buf: mut any*, n: size_t);
extern write(fd: s32, buf: any*, n: size_t);

let STDIN_FILENO: s32 = 0;
let STDOUT_FILENO: s32 = 1;
let STDERR_FILENO: s32 = 2;
