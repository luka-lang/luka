import "lib/stdio";

enum Weekday {
    Sunday = 1,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday
}

fn main(): s32 {
    let mut day = Weekday::Sunday;
    while day <= Weekday::Saturday {
        printf("Day of the week: %d\n", day);
        day = day + 1;
    }

    0
}
