extern puts(s: str);

fn add(a: int, b: int): int {
    if 1 > 0 {
        a + b
    }
    else {
        b - a
    }
}

fn returns_nothing(): void {
   1 + 2;
}

fn main(): int {
   puts("Hello World!");
}
